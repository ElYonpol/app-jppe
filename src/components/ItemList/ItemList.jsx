import Item from "../Item/Item";
import { Link } from "react-router-dom";

export default function ItemList(props){
    return(
        <div>
            {props.map((products) =>(

            ))}
        </div>
    )
}